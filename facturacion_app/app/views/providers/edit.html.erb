<h1>Edit Provider</h1>

<%= semantic_form_for @provider do |f| %>
  <%= f.input :name %>
  <%= f.input :phone %>
  <%= f.input :email %>
  <%= f.input :adress %>
<%= f.action :submit, :label => "Update", :button_html => { :class => "btn btn-primary"} %>
<% end %>