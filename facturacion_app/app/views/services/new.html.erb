<h1>New Service</h1>

<%= semantic_form_for @service do |f| %>
  <%= f.input :name %>
  <%= f.input :details %>
  <%= f.input :amount %>
  <%= f.action :submit, :label => "Submit", :button_html => { :class => "btn btn-primary"} %>
<% end %>