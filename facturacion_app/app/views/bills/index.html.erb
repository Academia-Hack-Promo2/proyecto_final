<div class="row">
  <div class="container">
    <div class="col-md-12">    
      <div class="panel panel-primary">
        <div class="panel-heading">
          <div class="row">  
            <div class="col-md-2">
             <span class="input-group-addon" id="basic-addon1"> <i class="fa fa-list-alt"></i> Bills List</span>
            </div>
            <div class="btn-group col-md-3 col-md-offset-7" role="group">
              <%= link_to "All", 'http://localhost:3000/bills', class:"btn btn-default" %>
              <%= link_to "Pendites", 'http://localhost:3000/bills?status=pendiente', class:"btn btn-default" %>
              <%= link_to "Canceladas", 'http://localhost:3000/bills?status=canceladas', class:"btn btn-default" %>
            </div>
          </div>
        </div>
        <div class="panel-body">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Client</th>
                <th>Rif</th>
                <th>Bill number</th>
                <th>Bill control</th>
                <th>quantity</th>
              </tr>
            </thead>
            <tbody>
              <% @bills.each do |bill| %>
              <tr>
                <td><%= bill.client %></td>
                <td><%= bill.rif %></td>
                <td><%= bill.bill_number %></td>
                <td><%= bill.bill_control %></td>
                <td><%= bill.quantity %></td>
                <td><%= link_to "Edit", edit_bill_path(bill), class:"btn btn-default" %></td>
                <td><%= link_to "Details", 'http://localhost:3000/bills/'+ bill.id, class:"btn btn-default" %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
          <%= link_to "Create Bill", new_bill_path, class:"btn btn-success" %>
        </div>
      </div>
    </div>
  </div>
</div>