<h1>Edit Service</h1>

<%= semantic_form_for @service do |f| %>
  <%= f.input :name %>
  <%= f.input :details %>
  <%= f.input :amount %>
<%= f.action :submit, :label => "Update", :button_html => { :class => "btn btn-primary"} %>
<% end %>