<div class="row">
  <div class="container">
    <div class="col-md-12">    
      <div class="panel panel-primary">
       <div class="panel-heading">
          <div class="row">  
            <div class="col-md-2">
             <span class="input-group-addon" id="basic-addon1"> <i class="fa fa-list-alt"></i> Providers Bills</span>
            </div>
            <div class="btn-group col-md-3 col-md-offset-7" role="group">
              <%= link_to "All", 'http://localhost:3000/provider_bills', class:"btn btn-default" %>
              <%= link_to "Pendites", 'http://localhost:3000/provider_bills?status=0', class:"btn btn-default" %>
              <%= link_to "Canceladas", 'http://localhost:3000/provider_bills?status=1', class:"btn btn-default" %>
            </div>
          </div>
        </div>
        <div class="panel-body">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Bill Numbee</th>
                <th>Description</th>
                <th>Amount</th>
                <th>Status</th>
                <th>Payment Number</th>
                <th>Provider id</th>
              </tr>
            </thead>
            <tbody>
              <% @p_bills.each do |p_bill| %>
              <tr>
                <td><%= p_bill.bill_number %></td>
                <td><%= p_bill.description %></td>
                <td><%= p_bill.amount %></td>
                <td><%= p_bill.status %></td>
                <td><%= p_bill.payment_number %></td>
                <td><%= p_bill.provider_id %></td>
                <td><%= link_to "Edit", edit_provider_bill_path(p_bill), class:"btn btn-default" %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
          <%= link_to "Create Provider Bill", new_provider_bill_path, class: "btn btn-success" %>
        </div>
      </div>
    </div>
  </div>
</div>